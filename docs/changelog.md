# Changelog

This file is auto-generated by `prv-cli living update`.

## From living.toml

- : Use virtual workspace with resolver 2 (``)
- : 8-dimensional state vector with nalgebra SVector (``)
- : Finite-difference Jacobians via central differences (``)
- : Quaternion model is experimental with baseline comparison invariant (``)
- : living.toml at root as structured state, Markdown projections in docs/ (``)
- : ObservationModel trait generic over const D for dataset-dependent dimensions (``)
- : CLI living update reads from living.toml + spec.toml + specs/*.toml instead of hardcoding (``)
- : to_observations returns first 10 required columns as 10-dim Observation vector (``)
- : DataLoader::load_historical skips 'quarter' header column by name during float parsing (``)
- : Verified Rust 1.98.1 with edition 2024 support (``)
- : Verified thesix 0.2.3 on crates.io (``)
- : Verified themql-runtime 0.1.0 + themql-core on crates.io (``)
- : Created workspace directory structure (``)
- : Written all spec TOML files (``)
- : Converted Cargo.toml to virtual workspace with verified dependency versions (``)
- : Implemented prv-core: State, Observation<D>, Regime, PressureReleaseValve, TimeSeries (``)
- : Implemented prv-filter: Ekf<M,O,D>, TransitionModel, ObservationModel traits (``)
- : Implemented prv-monte-carlo: ShockSpec, Simulator with Cholesky sampling (``)
- : Implemented prv-policy: PolicyEngine, PolicyDistribution, regime_response (``)
- : Implemented prv-geometry: QuaternionState, baseline_compare (``)
- : Implemented prv-data: DataFrame, DataLoader (``)
- : Implemented prv-evaluation: Model trait, Evaluator, metrics (``)
- : Implemented prv-cli: clap Commands with spec/living/session/status stubs (``)
- : Updated AGENTS.md with PRV Spec-and-Go context (``)
- : cargo check --workspace passes with zero warnings (``)
- : cargo clippy --workspace -- -D warnings passes (``)
- : cargo test --workspace passes (``)
- : cargo fmt --check passes (``)
- : Added 26 unit tests: prv.rs (3), observation.rs (3), transition.rs (3), shocks.rs (3), metrics.rs (7), backtest.rs (3), ekf.rs (1), loader.rs (3) (``)
- : prv-cli living update reads from living.toml + spec.toml + specs/*.toml (no hardcoded strings) (``)
- : prv-cli session handover reads from living.toml sections (completed/changed/validated/remaining) (``)
- : prv-cli status reads phase/status/next_action from living.toml (``)
- : cargo test --workspace passes with 72 tests (16 core + 8 data + 18 evaluation + 12 filter + 5 geometry + 8 monte_carlo + 3 policy) (``)
- : Added TETANUS enforcement: scripts/tetanus-check.py, clippy.toml, tetanus.toml, workspace lints in root Cargo.toml (``)
- : Strict crate lib.rs headers updated with TETANUS deny lints (core/filter/monte_carlo/policy/geometry) (``)
- : Advisory crate lib.rs headers cleaned (data/evaluation/cli) (``)
- : Removed unwrap/expect from strict crate lib code: simulator.rs sample_state/simulate, engine.rs sample_noise/evaluate (``)
- : Added supply-chain gates: deny.toml, .cargo/audit.toml (``)
- : Created prv-cache crate with thesix integration, NsCache, CacheStats, KeyIndex (``)
- : Integrated NsCache into DataLoader via load_historical_cached() (``)
- : Created SCHEMA.md, scripts/check_schema.sh, examples/helix-query.json (``)
- : Created .github/workflows/ci.yml with all quality gates (``)
- : Updated AGENTS.md with Quality Gates section (``)
- : Fixed DataLoader::load_historical to skip non-numeric 'quarter' column during CSV float parsing (``)
- : Verified full pipeline example (cargo run --example run_pipeline -p prv-cli) executes end-to-end with faux_data.csv (``)
- : Published all 9 workspace crates to crates.io as prv-*-0.2.0 (``)
- : Fixed remaining clippy errors in run_pipeline.rs: removed format! borrow, replaced io::Error::new with Error::other, removed redundant closure (``)
- : Added bzip2-1.0.6 to deny.toml licenses to satisfy cargo deny after docx_basic dependency (``)
- : Verified full quality gate suite passes: cargo fmt, cargo clippy, 77 tests, TETANUS 0 errors, cargo deny check ok (``)
- : Verified --save report export works end-to-end for markdown, txt, toml, and docx formats (``)
- : Fixed all 24 clippy warnings in run_pipeline.rs example: replaced unwrap with expect, fixed format! args, added allows for cast_precision_loss and suboptimal_flops in generate_faux_csv (``)
- : Fixed DataLoader invariant: load_historical now removes 'quarter' from both headers and rows so columns.len() == row.len() (``)
- : Fixed to_observations() to select observation values by required series name rather than positional index, making it robust to column reordering (``)
- : Added prv-cache to publish-next-crate.sh release ordering (was missing from 9-crate publish list) (``)
- : Added crates.io publication verification step to CI workflow that runs on tags (``)
- : Fixed Markdown Monte Carlo report bug: generate_markdown_report now uses mc_mean.as_vector() instead of reusing EKF dims array (``)
- : Narrowed Clippy blanket exemptions in run_pipeline.rs: removed used_underscore_binding, too_many_lines, expect_used, use_debug, let_underscore_must_use, too_many_arguments, single_char_add_str, format_push_string; added inline #[allow] with TETANUS-exempt comments (``)
- : Bumped workspace version 0.2.0 -> 0.2.1 in Cargo.toml and spec.toml (``)
- : Created git tag v0.2.1 and pushed to origin (``)
- : Created GitHub release v0.2.1 with PR2 review fixes summary (``)
- : Published all 9 workspace crates to crates.io as v0.2.1 (``)
- : PR #2 merged: all quality gates green, release complete (``)
- : Rust toolchain 1.98.1 confirmed (``)
- : Dependencies verified on crates.io (``)
- : Workspace compiles with zero warnings (``)
- : Clippy pedantic + nursery passes with -D warnings (``)
- : cargo fmt --check passes (``)
- : cargo test --workspace: 79 tests pass (``)
- : cargo deny check passes (advisories, bans, licenses, sources) (``)
- : tetanus-check.py passes with 0 errors (``)
- : cargo publish --dry-run passes for all 9 crates (``)
- : All 9 crates published to crates.io as v0.2.0 (``)
- : All remaining clippy errors in run_pipeline.rs example fixed (format! borrows, io::Error::other, redundant closure) (``)
- : cargo deny check passes after adding bzip2-1.0.6 license for docx_basic transitive dep (``)
- : Multi-format report export (--save) verified for markdown, txt, toml, and docx (``)
- : All 24 clippy warnings in run_pipeline.rs example fixed (0 warnings in workspace) (``)
- : DataLoader invariant restored: load_historical removes quarter from headers and rows so columns.len() == row.len() (``)
- : to_observations() selects by required series name rather than positional index, robust to column reordering (``)
- : publish-next-crate.sh includes prv-cache in release ordering (``)
- : CI workflow added crates.io publication verification step on tags (``)
- : Created GitHub release v0.2.1 at https://github.com/Metis-Avionics/economic-prv/releases/tag/v0.2.1 (``)
- : Published all 9 workspace crates to crates.io as v0.2.1 (``)
- : PR #2 merged into master with adversarial review; release v0.2.1 live on GitHub and crates.io (``)
- spec_created: Initial spec.toml with verified dependencies (``)
- dependency_resolved: theSix -> thesix 0.2.3 (crates.io) (``)
- dependency_resolved: theMQL -> themql-runtime 0.1.0 + themql-core (crates.io) (``)
- workspace_scaffolded: 8 crates scaffolded with lib.rs and Cargo.toml (``)
- core_implemented: prv-core: State, Observation<D>, Regime, PRV, TimeSeries, PrvError (``)
- filter_implemented: prv-filter: EKF with finite-difference Jacobians (``)
- monte_carlo_implemented: prv-monte-carlo: ShockSpec, Simulator with Cholesky sampling (``)
- policy_implemented: prv-policy: PolicyEngine, PolicyDistribution, regime_response (``)
- geometry_implemented: prv-geometry: QuaternionState with baseline_compare (``)
- data_implemented: prv-data: DataFrame, DataLoader (``)
- evaluation_implemented: prv-evaluation: Model trait, Evaluator, metrics (``)
- cli_implemented: prv-cli: clap Commands with spec/living/session/status (``)
- tests_added: Added 26 unit tests: prv.rs, observation.rs, transition.rs, ekf.rs, shocks.rs, metrics.rs, backtest.rs, loader.rs (``)
- cli_improved: prv-cli living update + session handover read from living.toml + specs instead of hardcoding (``)
- quality_gate: 72 tests pass; clippy -D warnings clean; fmt clean (``)
- tetanus_enforcement: Added TETANUS checker, clippy.toml, tetanus.toml, workspace lints, strict crate deny headers (``)
- supply_chain_gate: Added deny.toml and .cargo/audit.toml (``)
- cache_integration: Created prv-cache crate with thesix integration and wired into DataLoader (``)
- schema_added: Added SCHEMA.md, check_schema.sh, and helix-query.json example (``)
- ci_added: Created .github/workflows/ci.yml with all quality gates (``)
- loader_fix: Fixed DataLoader::load_historical to skip 'quarter' column; pipeline example now runs end-to-end (``)
- example_fix: Fixed prv-cli run_pipeline example: unused import, clippy allows, workspace deps in cli/Cargo.toml (``)
- version_bump: Bumped workspace version from 0.1.0 to 0.2.0 for PR2 (``)
- crates_io_publish: Published all 9 workspace crates to crates.io as v0.2.0 (``)
- cli_fix: Fixed remaining clippy errors in run_pipeline.rs: removed format! borrows, replaced io::Error::new with Error::other, removed redundant closure (``)
- supply_chain_fix: Added bzip2-1.0.6 license to deny.toml to satisfy cargo deny after docx_basic dependency (``)
- quality_gate: All quality gates pass: fmt clean, clippy clean, 77 tests pass, TETANUS 0 errors, cargo deny check ok (``)
- report_export: Added multi-format report export (--save) to run_pipeline.rs with md, txt, toml, and docx outputs (``)
- clippy_clean: Fixed all 24 clippy warnings in run_pipeline.rs example: expect(), format! args, cast_precision_loss and suboptimal_flops allows (``)
- loader_invariant_fix: DataLoader::load_historical now removes 'quarter' from both headers and rows, preserving DataFrame invariant columns.len() == row.len() (``)
- observation_name_fix: to_observations() now selects observation values by required series name rather than positional index, robust to column reordering (``)
- publish_script_fix: Added prv-cache to publish-next-crate.sh CRATES array for correct 9-crate release ordering (``)
- ci_verification: CI workflow now verifies all workspace crates exist on crates.io at the current version when tags are pushed (``)
